《大型平台软件设计实习》功能检查列表

***\*一、基础模块（必做）\****

***\*1. SQL编译器\****

1.1 词法分析器（Lexer）

支持识别 SQL 关键字（SELECT、FROM、WHERE、CREATE TABLE、INSERT INTO、DELETE 等）

支持识别标识符、常量、运算符、分隔符

输出 Token 格式：[种别码, 词素值, 行号, 列号]

支持错误提示（非法字符、位置）

 

1.2 语法分析器（Parser）

至少支持：CREATE TABLE、INSERT、SELECT、DELETE

构建抽象语法树（AST）

语法错误提示（位置 + 期望符号）

 

1.3 语义分析器

表/列存在性检查

类型一致性检查

列数/列序检查

维护系统目录（Catalog）

错误输出格式：[错误类型, 位置, 原因]

 

1.4 执行计划生成器

将 AST 转换为逻辑执行计划

支持算子：CreateTable、Insert、SeqScan、Filter、Project

输出格式：树形 / JSON / S表达式

 

1.4 测试与输出

支持多语句输入（文件/stdin）

输出各阶段结果（Token → AST → 语义 → 执行计划）

提供错误测试用例（缺分号、类型错误、列名错误等）

 

***\*2. 操作系统页面管理\****

2.1 页式存储系统

页大小固定（如 4KB）

支持页的分配、释放、读写

提供接口：read_page(page_id), write_page(page_id, data)

 

2.2 缓存机制

实现页缓存（如 LRU 或 FIFO）

支持缓存命中统计、替换日志

提供接口：get_page(page_id), flush_page(page_id)

 

2.3 接口与集成

提供统一存储访问接口

与执行计划对接，支持物理数据访问

 

***\*3. 数据库系统\****

3.1 执行引擎

实现算子：CreateTable、Insert、SeqScan、Filter、Project

支持条件查询（WHERE）

 

3.2 存储引擎

调用页式存储接口

实现记录与页的序列化/反序列化

管理空闲页列表

 

3.3 系统目录

维护元数据（表结构、列信息）

目录本身作为一张表存储

 

3.4 CLI/API/Web

支持用户输入 SQL 语句

返回执行结果或错误信息

 

3.5 数据持久化

重启后数据不丢失

所有数据通过页式存储系统保存

 

3.6 测试与验证

覆盖建表、插入、查询、删除

验证条件查询、错误处理、持久化

 

***\*二、扩展模块（选做）\****

***\*1. SQL编译器扩展\****

支持 UPDATE 语句

支持 JOIN 操作

支持 ORDER BY / GROUP BY

查询优化（如谓词下推）

错误诊断增强（智能纠错提示）

 

***\*2. 存储系统扩展\****

支持多种缓存替换策略（如 Clock、LFU）

实现预读（Read-ahead）机制

支持日志恢复（WAL）

 

***\*3. 数据库系统扩展\****

支持事务（ACID）

支持索引（B+树）

支持多表连接查询

支持聚合函数（COUNT、SUM等）

支持命令行历史、自动补全

 

***\*4. 分布式数据库扩展\****

4.1 数据分片（Sharding）

支持按范围、哈希等方式进行数据分片

实现分片元数据管理

支持跨分片数据定位

 

4.2 分布式查询处理

支持分布式查询计划生成

实现跨节点数据合并（Merge）

支持分布式聚合计算

 

4.3 数据复制与一致性

支持主从复制机制

实现最终一致性或弱一致性模型

支持读写分离

 

4.4 分布式事务

实现两阶段提交（2PC）协议

支持分布式死锁检测

提供事务隔离级别支持

 

4.5 容错与高可用

支持节点故障检测

实现数据副本自动恢复

支持负载均衡

 

4.6 分布式系统协调

集成分布式协调服务（如模拟ZooKeeper功能）

实现集群成员管理

支持配置信息同步

 

4.7 性能监控与调优

实现分布式查询性能分析

支持慢查询日志

提供系统运行状态监控接口

 

***\*5. 其他扩展\****

 