# 多数据库功能测试指南

## 功能概述
SparrowDB现已支持多数据库功能，类似于Navicat的数据库管理界面。系统将原有的表、视图、函数等对象放在名为"main"的默认数据库中。

## 新增功能

### 1. 数据库管理界面
- **数据库选择下拉框**: 位于左侧面板顶部，可以选择当前活动的数据库
- **创建数据库按钮**: 点击可创建新数据库
- **删除数据库按钮**: 删除当前选中的数据库（main数据库不能删除）

### 2. 支持的SQL命令
- `CREATE DATABASE database_name` - 创建新数据库
- `DROP DATABASE database_name` - 删除数据库
- `USE database_name` - 切换到指定数据库
- `SHOW DATABASES` - 显示所有数据库列表

### 3. 数据库树显示
- 数据库树根节点现在显示当前数据库名称
- 显示格式：`数据库: database_name`

## 测试步骤

### 步骤1: 启动系统
1. 运行 `run_gui.bat`
2. 系统将自动连接到"main"数据库
3. 可以看到左侧面板顶部的数据库管理区域

### 步骤2: 测试创建数据库
1. 点击"创建数据库"按钮
2. 输入数据库名称，如："test_db"
3. 确认创建成功后，下拉框中应该出现新数据库

### 步骤3: 测试数据库切换
1. 使用下拉框选择不同的数据库
2. 观察数据库树根节点的变化
3. 或者使用SQL命令：`USE test_db`

### 步骤4: 测试SQL命令
```sql
-- 显示所有数据库
SHOW DATABASES;

-- 创建新数据库
CREATE DATABASE my_new_db;

-- 切换数据库
USE my_new_db;

-- 在新数据库中创建表
CREATE TABLE users (
    id INT PRIMARY KEY,
    name VARCHAR(50),
    email VARCHAR(100)
);

-- 切换回main数据库查看原有数据
USE main;
```

### 步骤5: 测试删除数据库
1. 选择非main数据库
2. 点击"删除数据库"按钮
3. 确认删除操作
4. 系统会自动切换回main数据库

## 目录结构变化
```
data/
├── main/              # 默认数据库（原有数据）
│   ├── *.tbl         # 表文件
│   └── __system_*.tbl # 系统表
├── test_db/          # 新创建的数据库
│   └── __system_*.tbl # 系统表
└── log/              # 日志目录
```

## 注意事项
1. main数据库不能被删除，这是系统的默认数据库
2. 数据库名称只能包含字母、数字、下划线，且不能以数字开头
3. 每个数据库都有独立的表、视图、函数空间
4. 切换数据库时，自动补全功能会更新为当前数据库的对象
5. 所有原有功能（表操作、视图、函数等）在每个数据库中都可以正常使用

## 兼容性
- 所有原有的SQL功能在各个数据库中都能正常工作
- 导入导出功能支持当前选中的数据库
- 系统表管理功能按数据库独立管理
